import { store as global } from "@/modules/global";
import { store as auth } from "@/modules/auth";
import { store as external } from "@/modules/external";
import { store as panel } from "@/modules/panel";

export default {
  global,
  auth,
  external,
  panel,
};
